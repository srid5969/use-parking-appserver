export enum AddressType {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  WORK = 'work',
  HOME = 'home',
}

export enum Status {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  DELETED = 'deleted',
}
